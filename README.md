# Luca Galli — personal website

A responsive React portfolio built with Vite.

## Run locally

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The site content lives in `src/App.jsx` and the visual system is in `src/styles.css`.
